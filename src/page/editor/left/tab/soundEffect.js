import React, { useState, useRef } from 'react';
import { connect } from 'dva';
import Empty from 'Components/empty';

/**
 * 音效
 */
function SoundEffect() {
    return (
        <Empty text={'敬请期待'} style={{marginTop: '100px'}} />
    );

} 



export default SoundEffect;